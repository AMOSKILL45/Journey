# Runtime Contracts — This Is The Journey

Externalised checklist for the contracts our automated tests **can't** catch (third-party
dashboards, native platform config, OAuth allowlists). Re-run before every production
push to TestFlight / Play Store / web.

Generated by `auditing-runtime-contracts` skill — 2026-05-26, refreshed after Phase 3.

> **Automated coverage** (static, runs in `npm test`):
>
> - `src/__tests__/deep-links-contract.test.ts` — every `thisisthejourney://…` literal must resolve to a screen
> - `src/__tests__/internal-routes-audit.test.ts` — every `router.push` / `<Redirect>` / `<Link>` must resolve to a screen
> - `src/__tests__/i18n-keys-contract.test.ts` — every `t('key.path')` resolves in both en + fr
> - `src/__tests__/asset-requires-contract.test.ts` — every `require('./image.png')` resolves to a file on disk
>
> **Runtime coverage** (smoke, runs against a booted sim/device):
>
> - `.maestro/sign-in.yaml` — boot + magic-link entry flow
> - `.maestro/deep-link-callback.yaml` — magic-link tap doesn't 404
> - `.maestro/deep-link-invite.yaml` — invite tap doesn't 404
>
> **The list below is the manual residue** — what neither test catches.

---

## 1. EAS Environment Variables (production)

`src/core/env/index.ts` validates these via zod. If a required var is missing in the EAS
production environment, the app **throws at boot** and crashes silently on TestFlight.

Run `eas env:list production` and confirm:

### Required (zod throws if missing)

- [x] `EXPO_PUBLIC_SUPABASE_URL` = `https://ewsoupkfkachxidmuwoi.supabase.co` _(set 2026-05-25)_
- [x] `EXPO_PUBLIC_SUPABASE_ANON_KEY` = `sb_publishable_hVXos8ENzKBYYzSCkQPLvQ_BkyjMDb0` _(set 2026-05-25)_

### Optional (consumed but `.optional()` in zod — won't crash if missing, just degraded)

- [ ] `EXPO_PUBLIC_SENTRY_DSN` — without this, prod crashes won't reach Sentry. **TODO: create Sentry project.**
- [ ] `EXPO_PUBLIC_POSTHOG_API_KEY` — without this, analytics events are dropped. **TODO: create PostHog project.**
- [ ] `EXPO_PUBLIC_POSTHOG_HOST` — defaults to `https://us.i.posthog.com` if unset (fine).
- [ ] `EXPO_PUBLIC_MAPTILER_API_KEY` — **REQUIRED as of Phase 3.** Without it: (a) milestone geocoding fails silently in the creation sheet; (b) the MapLibre Real Map layer renders a blank/grey tile area because the vector source URL is empty; (c) glyph (label font) fetches 401 — text labels disappear. Wired by `src/features/map/utils/cozyMapStyle.ts:31` + `src/features/milestones/api/geocoding.ts:46`. _Free tier 100k req/month is plenty for v1._
- [ ] `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` — without this, Google Sign-In throws on first tap.
- [ ] `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` — same.
- [ ] `EXPO_PUBLIC_GOOGLE_IOS_URL_SCHEME` — without this, the Google plugin is NOT included in the native build (see `app.config.ts:13`). Google Sign-In button will be present but tap will fail.

Repeat for `preview` and `development` environments if you build for them.

---

## 2. Supabase Auth — URL Configuration

Dashboard: <https://supabase.com/dashboard/project/ewsoupkfkachxidmuwoi/auth/url-configuration>

The literal `thisisthejourney://auth/callback` is passed as `emailRedirectTo` in
`src/features/auth/api/auth.ts:24`. If this is not allowlisted, Supabase silently
falls back to the Site URL (which by default is `http://localhost:3000` 👈).

- [x] **Site URL** = `thisisthejourney://auth/callback` _(set 2026-05-26)_
- [x] **Redirect URLs** contains `thisisthejourney://auth/callback` _(set 2026-05-26)_
- [x] **Redirect URLs** contains `thisisthejourney://**` _(wildcard for invite tokens, password reset, etc.)_

To re-verify the exact list the code will emit:

```bash
grep -rE "emailRedirectTo|redirectTo" src/features/auth/api/
```

---

## 3. Google OAuth (when shipping Google Sign-In)

Currently inactive (env vars empty). When you ship it:

- [ ] Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
- [ ] Create **Web client** → copy ID into `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` (used by Supabase to verify the ID token)
- [ ] Create **iOS client** with bundle ID `com.thisisthejourney.app` → copy:
  - Client ID → `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`
  - URL scheme (reverse-DNS form of client ID) → `EXPO_PUBLIC_GOOGLE_IOS_URL_SCHEME`
- [ ] In Supabase dashboard → Authentication → Providers → Google → enable + paste **Web client ID** + **Web client secret**

---

## 4. Apple Sign In

`app.config.ts:53` declares `usesAppleSignIn: true`. The plugin
`expo-apple-authentication` is in `corePlugins`. This is sufficient for the native
side, but Supabase also needs to know.

- [ ] Apple Developer → Identifiers → App IDs → `com.thisisthejourney.app` → Sign In with Apple enabled
- [ ] Apple Developer → Identifiers → Services IDs → create a Service ID for Supabase
  - Return URL: `https://ewsoupkfkachxidmuwoi.supabase.co/auth/v1/callback`
- [ ] Supabase dashboard → Authentication → Providers → Apple → enabled + Service ID + key

---

## 5. App Store Connect

- [x] `eas.json` → `submit.production.ios.ascAppId` = `6773123707` _(set 2026-05-26)_
- [ ] App Store Connect → My Apps → "This Is The Journey" → TestFlight → Internal Testing → at least one tester added (otherwise even successful uploads aren't visible to test)
- [ ] App Information → Privacy Policy URL set _(required for TestFlight external testing)_
- [ ] Encryption export compliance: `ITSAppUsesNonExemptEncryption: false` ✅ already in `app.config.ts:51`

---

## 6. Native config sanity (Expo prebuild)

These need to match what the code asserts. Verified via `app.config.ts`:

- [x] `scheme: 'thisisthejourney'` ← matches every `thisisthejourney://*` literal in code
- [x] `ios.bundleIdentifier: 'com.thisisthejourney.app'` ← matches certificates
- [x] `android.package: 'com.thisisthejourney.app'`
- [x] `newArchEnabled: true` ← required for Reanimated 4 / React 19
- [x] `expo-build-properties` plugin → `ios.useFrameworks: 'static'` ← required for Sentry + Google Sign In coexistence

If you add a new deep-link URL with a different scheme (e.g. `https://thisisthejourney.app/…`
universal links), you'll also need:

- [ ] iOS Associated Domains entitlement
- [ ] `apple-app-site-association` JSON at `https://thisisthejourney.app/.well-known/apple-app-site-association`
- [ ] Android Digital Asset Links at `https://thisisthejourney.app/.well-known/assetlinks.json`

---

## 7. Database schema ↔ code

The Supabase generated types (`src/core/supabase/types.generated.ts`, gitignored) tie
SQL columns to TS types. To keep these in sync:

- [ ] Whenever a migration is applied: run `npm run supabase:types` and commit _(actually, the file is gitignored, so this is local-only — but the types must regen for TS to pick up new columns)_

### Pending migrations (apply via Supabase MCP next session)

- [ ] `supabase/migrations/20260525000003_security_hardening.sql` _(carried over from Phase 1)_
- [ ] `supabase/migrations/20260526120001_milestones_latlng.sql` — Phase 3 T2. Adds `lat` / `lng` GENERATED columns on `milestones`. Without applying, the map layers compile-time pass (TS bridge via `MilestoneWithCoords` cast) but queries return `lat: undefined / lng: undefined` and the map renders an empty space. Apply on the remote DB **before** any user sees the Map tab in TestFlight.

---

## 8. MapLibre / MapTiler (Phase 3)

Dashboard: <https://cloud.maptiler.com/account/keys/>

- [ ] Create a key (free tier) and add it to `EXPO_PUBLIC_MAPTILER_API_KEY` in EAS env + local `.env`.
- [ ] Allowed domains / bundle IDs on the MapTiler key:
  - iOS bundle: `com.thisisthejourney.app`
  - Android package: `com.thisisthejourney.app`
  - Localhost (dev): not needed for native, but helps if we ever ship web.
- [ ] Monthly quota alert at ~80 % of free tier (100 k requests).
- [ ] The MapLibre Expo config plugin is registered in `app.config.ts:11`. Running `npx expo prebuild --clean` is **required** the next time native is regenerated — adding a plugin is not OTA-shippable.

---

## 9. Native build vs OTA update

Changes that **require a new native build** (`eas build` + store upload, NOT OTA):

- New Expo config plugins (`@maplibre/maplibre-react-native` added Phase 3).
- New native deps without an Expo plugin (`@shopify/react-native-skia` added Phase 3).
- New iOS Info.plist keys (`NSLocationWhenInUseUsageDescription` added Phase 3).
- New URL schemes / Associated Domains / intent-filters.

Changes that **can ship OTA** (`eas update`):

- Pure JS / TS changes (any `src/**` file that doesn't touch `app.config.ts`).
- New translations, new screens, new components, new business logic.
- Migration files in `supabase/migrations/**` — these apply server-side independently.

**Heuristic for Phase 3**: native bits landed (MapLibre + Skia + Info.plist) → next ship needs a build, not an OTA. After that, JS-only follow-ups are OTA-able again.

---

## How to use this file

- **Adding a new env var** → add it to section 1, set it via `eas env:create`, commit this file.
- **Adding a new OAuth provider** → add a section (copy 3 or 4 as template), commit.
- **Before each prod push** → walk the list, check anything that changed since last push.
- **After a runtime bug shipped** → root-cause it, then add the missing contract here so it can't repeat.
